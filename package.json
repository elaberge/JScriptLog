{
  "name": "JScriptLog",
  "version": "0.8.0",
  "description": "JavaScript Prolog interpreter",
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "files": [
    "src",
    "dist/src"
  ],
  "dependencies": {
    "@types/node": "^7.0.5",
    "source-map-support": "^0.4.11"
  },
  "devDependencies": {
    "@types/chai": "^3.4.31",
    "@types/chai-as-promised": "0.0.29",
    "@types/mocha": "^2.2.30",
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "gaze-run-interrupt": "^1.0.1",
    "mocha": "^3.1.2",
    "rimraf": "^2.5.2",
    "source-map-support": "^0.4.11",
    "tslint": "4.4.2",
    "typescript": "2.1.5"
  },
  "scripts": {
    "prepublish": "npm run build",
    "clean": "rimraf dist",
    "build": "npm run clean && jsonlint -i package.json && tsc && tsc --p tsconfig.html.json",
    "test": "npm run build && tslint --project tsconfig.json && mocha 'dist/test/**/*Spec.js' --require source-map-support/register"
  }
}